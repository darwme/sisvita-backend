#spring.application.name=241CC341SpringMSDemo01ConfigD
server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
        #search-locations: classpath:/configurations